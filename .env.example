# Server Configuration
PORT=8080
ENVIRONMENT=development

# Database Configuration
# Options: sqlite, postgres
DB_TYPE=sqlite

# SQLite (default)
DB_DSN=./data/kuma.db

# PostgreSQL (alternative)
# DB_DSN=postgresql://kuma:kuma@localhost:5432/kuma?sslmode=disable

# Database Connection Pool
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# Security
# CRITICAL: Generate a strong random secret for production!
# Use: openssl rand -base64 32
# Minimum 32 characters required in production
JWT_SECRET=change-this-secret-in-production

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: https://monitor.example.com,https://app.example.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
